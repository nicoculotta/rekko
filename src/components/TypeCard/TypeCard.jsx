import React, { useState } from "react";
import "./TypeCard.css";

const TypeCard = ({ title }) => {
  return <h4 className="home__carrousel-title heading heading-2">{title}</h4>;
};
export default TypeCard;
